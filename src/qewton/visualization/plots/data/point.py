from qewton.visualization.plots.base import Plot


class PointPlot(Plot):
    """Unstructured points without stored connections - values shown by
    coloring the points or elevating them along an additional axis."""
